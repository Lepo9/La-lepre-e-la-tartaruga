## Changelog
Qua si scrive i cambiamenti al codice.

*********************************
30/11/2020

Definizione delle funzioni da scrivere

*********************************
01/12/2020

uptate 0.2

Aggiustamento e commento delle funzioni:
bool positivo (int n)
int spostamento_tarta ()

Aggiunta fuori dal main di:
#include <ctime>
#include <cstdlib>

Aggiunta a inizio main:
srand (time(NULL));

Da revisionare:
bool credito_sufficiente (int credito, int puntata)
int spazi (int n)

*********************************

02/12/2020

update 0.3

Revisione delle funzioni: 
bool credito_sufficiente (int credito, int puntata)
int spazi (int n) --> void spazi (int n)

Scrittura delle funzioni:
int spostamento_lepre () 
int gettoni_vinti (int gettoni_puntati, bool flag_scommessa, bool flag_vincitore) 
void disegno_tarta (int avanzamento_tarta, int passi_totali)
void disegno_lepre (int avanzamento_tarta, int passi_totali)

*********************************

03/12/2020

update 0.4

Scrittura struttura generale del main: 

Introduzione delle seguenti stringhe:
  #include <windows.h>    //libreria per sleep 
  Sleep (intervallo_fotogrammi); //la funzione aspetta intervallo_fotogrammi millisecondi
  system ("pause"); //viene richiamata la funzione pause per aspettare che l'utente abbia le regole
  system ("cls"); //viene richiamata la funzione cls per cancellare la shell

Aggiunta delle variabili:
    int credito; //credito totale
    bool flag_giocare = true; //flag per riocare
    int passi_lepre = 0, passi_tarta = 0; //contatori passi
    const int passi_totali = 100; //passi totali
    const int intervallo_fotogrammi = 350; //intervallo dei fotogrammi
    
    
*********************************

04/12/2020

update 0.5

Scrittura generale del main:

system ("pause"); //viene richiamata la funzione pause per aspettare che l'utente abbia le regole
system ("cls"); //viene richiamata la funzione cls per cancellare la shell
system ("color ");//viene richiamta la funzione color per modificare il colore del testo
int scommessa(gettoni)
int scelta(0,1) (0 tartargura - 1 lepre)

Aggiunta aggiustaento parte grafica del traguardo in caso di pareggio. [Leporati e Ragnoli]
Rimozione della funzione gettoni vinti 

Inserimento delle variabili e funizioni per la parte finale del ciclo responsabile delle partite:
  void guadagno (int credito_finale, int credito_iniziale) //stampa quanti gettoni si ha vinto dall'inizio o si ha perso
  int credito_iniziale; //è il credito che si aveva inserito all'inizio
  int contatore_vittorie = 0, contatore_sconfitte = 0; //
